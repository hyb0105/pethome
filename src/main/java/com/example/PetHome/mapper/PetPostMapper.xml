<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.PetHome.mapper.PetPostMapper">

    <resultMap id="PetPostResultMap" type="com.example.PetHome.entity.PetPost">
        <result property="petName" column="pet_name"/>

        <result property="likedByCurrentUser" column="liked_by_current_user"/>
    </resultMap>

    <insert id="insertPost" parameterType="com.example.PetHome.entity.PetPost" useGeneratedKeys="true" keyProperty="id">
    </insert>

    <select id="findPostById" resultMap="PetPostResultMap">
        SELECT p.*, (pl.user_id IS NOT NULL) AS liked_by_current_user
        FROM pet_post p
                 LEFT JOIN post_like pl ON p.id = pl.post_id AND pl.user_id = #{currentUserId}
        WHERE p.id = #{id}
    </select>

    <select id="findPostDetailById" resultMap="PetPostResultMap">
        SELECT p.*, u.username AS author_name,
               (pl.user_id IS NOT NULL) AS liked_by_current_user
        FROM pet_post p
                 LEFT JOIN user u ON p.author_id = u.id
                 LEFT JOIN post_like pl ON p.id = pl.post_id AND pl.user_id = #{currentUserId}
        WHERE p.id = #{id}
    </select>

    <select id="findAllPosts" resultMap="PetPostResultMap">
        SELECT p.*, u.username AS author_name,
        (pl.user_id IS NOT NULL) AS liked_by_current_user
        FROM pet_post p
        LEFT JOIN user u ON p.author_id = u.id
        LEFT JOIN post_like pl ON p.id = pl.post_id AND pl.user_id = #{currentUserId}
        <where>
            <if test="status != null"> AND p.status = #{status} </if>
            <if test="category != null and category != ''"> AND p.category = #{category} </if>
            <if test="authorId != null"> AND p.author_id = #{authorId} </if>
            <if test="title != null and title != ''"> AND p.title LIKE CONCAT('%', #{title}, '%') </if>
        </where>
        ORDER BY p.create_time DESC
    </select>

    <update id="updatePostStatus">
    </update>

    <delete id="deletePostById">
    </delete>

    <update id="updateLikeCount">
        UPDATE pet_post SET likes = likes + #{amount} WHERE id = #{postId}
    </update>

    <update id="incrementViewCount">
        UPDATE pet_post SET views = views + 1 WHERE id = #{postId}
    </update>

</mapper>