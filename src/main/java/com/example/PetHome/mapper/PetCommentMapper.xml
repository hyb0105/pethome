<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.PetHome.mapper.PetCommentMapper">

    <resultMap id="CommentResultMap" type="com.example.PetHome.entity.PetComment">
        <id property="id" column="id"/>
        <result property="petId" column="pet_id"/>
        <result property="userId" column="user_id"/>
        <result property="content" column="content"/>
        <result property="createTime" column="create_time"/>
        <result property="authorName" column="author_name"/>
        <result property="authorAvatar" column="author_avatar"/>
        <result property="petName" column="pet_name"/>
    </resultMap>

    <insert id="insertComment" parameterType="com.example.PetHome.entity.PetComment">
        INSERT INTO pet_comment (pet_id, user_id, content)
        VALUES (#{petId}, #{userId}, #{content})
    </insert>

    <select id="findCommentsByPetId" resultMap="CommentResultMap">
        SELECT c.*, u.username AS author_name, u.avatar AS author_avatar
        FROM pet_comment c
                 LEFT JOIN user u ON c.user_id = u.id
        WHERE c.pet_id = #{petId}
        ORDER BY c.create_time DESC
    </select>

    <select id="findAllComments" resultMap="CommentResultMap">
        SELECT c.*, u.username AS author_name, u.avatar AS author_avatar, p.name AS pet_name
        FROM pet_comment c
        LEFT JOIN user u ON c.user_id = u.id
        LEFT JOIN pet p ON c.pet_id = p.id
        <where>
            <if test="content != null and content != ''">
                AND c.content LIKE CONCAT('%', #{content}, '%')
            </if>
            <if test="authorName != null and authorName != ''">
                AND u.username LIKE CONCAT('%', #{authorName}, '%')
            </if>
        </where>
        ORDER BY c.create_time DESC
    </select>

    <select id="findCommentById" resultMap="CommentResultMap">
        SELECT * FROM pet_comment WHERE id = #{id}
    </select>

    <delete id="deleteCommentById">
        DELETE FROM pet_comment WHERE id = #{id}
    </delete>
    <delete id="deleteCommentsByPetId">
        DELETE FROM pet_comment WHERE pet_id = #{petId}
    </delete>
</mapper>