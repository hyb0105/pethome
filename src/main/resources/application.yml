spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pet_adoption_system?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  mybatis:
    mapper-locations: classpath:mapper/*.xml
  servlet:
    multipart:
      enabled: true # 开启文件上传支持
      max-file-size: 10MB # 设置单个文件最大为10MB
      max-request-size: 100MB # 设置单次请求总大小为100MB

# 【新增】自定义配置项，用于指定文件存储的物理路径
file:
  upload-dir: C:/Users/hyb/Desktop/毕设/image/ # 您可以修改为您希望存储图片的本地路径

logging:
  level:
    org.springframework.security: DEBUG
    com.example.PetHome.mapper: TRACE